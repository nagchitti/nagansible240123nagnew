---
- hosts: webserver
  become: yes
  tasks:
  - name: i want to install java on ubuntu ansible nodes
    apt:
     name: "{{javaubuntu}}"
     state: present
    when: ansible_os_family =="Debian"
  - name: Install Java 8 on Red Hat
    yum:
     name: java-1.8.0-openjdk
     state: present

